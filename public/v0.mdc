---
description: 
globs: 
alwaysApply: false
---
**Typography:** Tailwind text scale + `font-sans`. Headings: `font-semibold`, buttons: `font-medium`. No px/brackets.

**Spacing:** Use 8px scale only.

**Components:** Pills for status, cards 270×270, PDP with SKU, price, CTA.

**Rules:** No inline styles, px, custom fonts/colors. Tokens must not change unless requested and confirmed.
